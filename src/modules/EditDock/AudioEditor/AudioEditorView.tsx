
import React from 'react';

export const AudioEditorView: React.FC = () => {
    return (
        <div className="w-full h-full flex items-center justify-center text-gray-500">
            Audio Editor View (Placeholder)
        </div>
    );
};
